# 🚀 Инструкция по деплою на Vercel

## Подготовка завершена ✅

Проект готов к деплою:
- ✅ Git репозиторий инициализирован
- ✅ Все файлы закоммичены
- ✅ .gitignore настроен
- ✅ vercel.json оптимизирован
- ✅ Шрифт Montserrat подключен
- ✅ WhatsApp интеграция готова
- ✅ Админка включена

---

## Вариант 1: Деплой через Vercel CLI (Быстрый)

### 1. Установите Vercel CLI

```bash
npm install -g vercel
```

### 2. Войдите в аккаунт Vercel

```bash
vercel login
```

Следуйте инструкциям для входа через браузер.

### 3. Задеплойте проект

В папке `app/` выполните:

```bash
vercel
```

Следуйте инструкциям в терминале:
- Set up and deploy? → **Y** (Yes)
- Which scope? → Выберите свой аккаунт
- Link to existing project? → **N** (No)
- Project name? → `aqua-himiya` (или своё название)
- In which directory is your code? → `.` (текущая папка)

Vercel автоматически:
- Обнаружит проект (Vite)
- Установит зависимости
- Запустит build
- Задеплоит на временный URL

### 4. Production деплой

```bash
vercel --prod
```

Получите финальный production URL!

---

## Вариант 2: Деплой через Vercel Dashboard (Рекомендуется)

### 1. Создайте GitHub репозиторий

**Перейдите на GitHub:**
- Создайте новый репозиторий (например `aqua-himiya`)
- **НЕ** инициализируйте с README

**Подключите локальный репозиторий:**

```bash
# В папке app/
git remote add origin https://github.com/ВАШ_USERNAME/aqua-himiya.git
git branch -M main
git push -u origin main
```

### 2. Импортируйте на Vercel

1. Перейдите на https://vercel.com
2. Нажмите **"Add New Project"**
3. Выберите **"Import Git Repository"**
4. Найдите репозиторий `aqua-himiya`
5. Нажмите **"Import"**

### 3. Настройте проект

Vercel автоматически определит настройки:

- **Framework Preset:** Vite
- **Root Directory:** `./` (оставьте пустым)
- **Build Command:** `npm run build`
- **Output Directory:** `dist`
- **Install Command:** `npm install`

Нажмите **"Deploy"**!

### 4. Ожидайте деплой

Vercel:
- ✅ Установит зависимости
- ✅ Соберёт проект
- ✅ Задеплоит на production URL
- ✅ Автоматически настроит SSL

---

## После деплоя

### 1. Замените тестовый WhatsApp номер

В файлах:
- `src/sections/Services.tsx` (строка 88)
- `src/components/WhatsAppFAB.tsx` (строка 14)

Замените `79999999999` на реальный номер в формате: `7XXXXXXXXXX`

**Закоммитьте изменения:**

```bash
git add .
git commit -m "Update WhatsApp phone number"
git push
```

Vercel автоматически пересоберёт!

### 2. Настройте кастомный домен (опционально)

В Vercel Dashboard:
1. Выберите проект
2. Settings → Domains
3. Добавьте домен
4. Следуйте инструкциям по настройке DNS

---

## Доступ к админке

После деплоя админка будет доступна по адресу:

```
https://ваш-домен.vercel.app/admin
```

**Первый вход:**
1. Откройте `/admin`
2. Используйте дефолтные креды (если есть в коде)
3. **СРАЗУ СМЕНИТЕ ПАРОЛЬ** в настройках

---

## Важные URL после деплоя

- 🌐 **Главная:** `https://ваш-домен.vercel.app/`
- 🛍️ **Каталог:** `https://ваш-домен.vercel.app/catalog`
- 👨‍💼 **Админка:** `https://ваш-домен.vercel.app/admin`

---

## Troubleshooting

### Build ошибки

Если билд упал - проверьте логи в Vercel Dashboard.

### Шрифт не загружается

Очистите кеш браузера (Ctrl+F5) после деплоя.

### WhatsApp кнопка не работает

Проверьте номер телефона в формате `7XXXXXXXXXX` (без +, пробелов, скобок).

### Админка не открывается

Убедитесь что роутинг работает - `vercel.json` должен содержать rewrites.

---

## Мониторинг

В Vercel Dashboard доступно:
- 📊 Analytics (посещаемость)
- 🚀 Deployment logs
- ⚡ Performance metrics
- 🔍 Error tracking

---

## Поддержка

Проблемы с деплоем? Проверьте:
1. Логи в Vercel Dashboard
2. GitHub репозиторий подключён
3. Build command правильная
4. Output directory = `dist`

---

✅ **Проект готов к деплою!** Выберите вариант 1 или 2 и запускайте! 🚀
